spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ead
    username: matheus
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
